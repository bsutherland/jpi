GET     /                   controllers.JPIWSController.index

# Websocket interface
GET     /chat               controllers.JPIWSController.chat

# HTTP API
GET     /:postalCode        controllers.JPIController.address(postalCode: String)

GET     /assets/*file       controllers.Assets.at(path="/public", file)
